build:
  binary: kibble
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

release:
  # Repo in which the release will be created.
  # Default is extracted from the origin remote URL.
  github:
    owner: indiereign
    name: shift72-kibble
